# Skin Graphics Tattoo Studio

A high-end, dark-themed website for Skin Graphics Tattoo & Piercing Studio featuring a Gemini-powered Chat Booking Agent.

## Setup

1. Clone the repository
2. Run `npm install`
3. Run `npm run dev`

## Deployment

This project is configured to be deployed using Vite. Run `npm run build` to generate the production build.
